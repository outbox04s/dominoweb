"use client";import {FormEvent,useState} from "react";export default function ContactForm(){const[state,setState]=useState<"idle"|"sending"|"done"|"error">("idle");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setState("sending");const f=new FormData(e.currentTarget);const body=Object.fromEntries(f.entries());const r=await fetch("/api/leads",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(body)});setState(r.ok?"done":"error");if(r.ok)e.currentTarget.reset()}return <form className="leadForm" onSubmit={submit}><input name="company" tabIndex={-1} autoComplete="off" className="hp" aria-hidden="true"/><label>HỌ VÀ TÊN<input name="name" required minLength={2} autoComplete="name" placeholder="Tên của bạn"/></label><label>SỐ ĐIỆN THOẠI<input name="phone" required inputMode="tel" autoComplete="tel" placeholder="09xx xxx xxx"/></label><label>NHU CẦU<select name="need" defaultValue=""><option value="" disabled>Chọn nhu cầu</option><option value="new_build">Đang xây nhà mới</option><option value="renovation">Đang cải tạo bếp</option><option value="research">Đang tìm hiểu</option></select></label><label>LỜI NHẮN <span>(KHÔNG BẮT BUỘC)</span><textarea name="message" rows={4} placeholder="Điều bạn đang quan tâm..."/></label><input type="hidden" name="sourcePage" value="/lien-he"/><button disabled={state==="sending"}>{state==="sending"?"ĐANG GỬI...":"GỬI YÊU CẦU →"}</button>{state==="done"&&<p className="formStatus">DOMINO đã nhận thông tin. Đội ngũ tư vấn sẽ liên hệ với bạn.</p>}{state==="error"&&<p className="formStatus">Chưa thể gửi thông tin. Vui lòng kiểm tra lại số điện thoại và thử lại.</p>}</form>}