import {NextResponse} from "next/server";export async function POST(req:Request){try{const body=await req.json();if(body.company)return NextResponse.json({ok:true});const name=String(body.name||"").trim();const phone=String(body.phone||"").replace(/\D/g,"");if(name.length<2||!/^0\d{9,10}$/.test(phone))return NextResponse.json({error:"invalid_input"},{status:400});return NextResponse.json({error:"lead_storage_not_configured"},{status:503})}catch{return NextResponse.json({error:"invalid_request"},{status:400})}}