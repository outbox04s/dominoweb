import {redirect} from "next/navigation";import {createClient} from "@/lib/supabase/server";const labels:any={new:"Mới",contacted:"Đã liên hệ",consulting:"Đang tư vấn",appointment:"Đã hẹn",converted:"Đã chốt",not_suitable:"Không phù hợp"};export default async function LeadsPage(){const s=await createClient();const{data:{user}}=await s.auth.getUser();if(!user)redirect("/admin/login");const{data:profile}=await s.from("profiles").select("role,full_name").eq("id",user.id).maybeSingle();if(!profile)redirect("/");const{data:leads=[]}=await s.from("leads").select("id,need,status,source_type,source_page,created_at,contacts(name,phone)").order("created_at",{ascending:false}).limit(100);return <main className="adminLeads"><header><div><p className="eyebrow">DOMINO / CRM</p><h1>KHÁCH HÀNG</h1></div><p>{leads.length} lead gần nhất</p></header><section className="leadTable"><div className="leadRow leadHead"><span>Khách hàng</span><span>Nhu cầu</span><span>Nguồn</span><span>Trạng thái</span><span>Thời gian</span></div>{leads.map((l:any)=><div className="leadRow" key={l.id}><span><strong>{l.contacts?.name||"—"}</strong><small>{l.contacts?.phone||"—"}</small></span><span>{l.need||"Chưa xác định"}</span><span>{l.source_type||"—"}<small>{l.source_page||""}</small></span><span><i className={"status status-"+l.status}>{labels[l.status]||l.status}</i></span><span>{new Intl.DateTimeFormat("vi-VN",{dateStyle:"short",timeStyle:"short",timeZone:"Asia/Ho_Chi_Minh"}).format(new Date(l.created_at))}</span></div>)}</section></main>}