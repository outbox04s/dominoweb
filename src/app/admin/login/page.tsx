import {redirect} from "next/navigation";import {createClient} from "@/lib/supabase/server";async function login(formData:FormData){"use server";const s=await createClient();const email=String(formData.get("email")||"");const password=String(formData.get("password")||"");const{error}=await s.auth.signInWithPassword({email,password});if(!error)redirect("/admin/leads")}export default function AdminLogin(){return <main className="adminLogin"><form action={login}><p className="eyebrow">DOMINO / ADMIN</p><h1>ĐĂNG NHẬP</h1><label>Email<input name="email" type="email" required/></label><label>Mật khẩu<input name="password" type="password" required/></label><button>ĐĂNG NHẬP →</button></form></main>}