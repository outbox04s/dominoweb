import Link from "next/link";import {createClient} from "@/lib/supabase/server";export const metadata={title:"Tin tức & Kiến thức | DOMINO",description:"Kiến thức tủ bếp, công trình, hoạt động và thông tin từ DOMINO."};export default async function Page(){const s=await createClient();const{data}=await s.from("posts").select("id,slug,title,excerpt,published_at").eq("status","published").order("published_at",{ascending:false}).limit(12);return <main className="editorialPage"><header className="editorialHero"><p className="eyebrow">DOMINO / JOURNAL</p><h1>TRƯỚC KHI<br/>LÀM BẾP.</h1><p>Những điều nên hiểu trước khi lựa chọn cấu tạo, phụ kiện và đơn vị đồng hành.</p></header><section className="journalGrid">{data?.length?data.map((p,i)=><Link href={"/tin-tuc/"+p.slug} className="journalCard" key={p.id}><span>{String(i+1).padStart(2,"0")}</span><small>{p.published_at?new Date(p.published_at).toLocaleDateString("vi-VN"):"DOMINO"}</small><h2>{p.title}</h2><p>{p.excerpt}</p><b>ĐỌC →</b></Link>):<div className="emptyEditorial"><span>DOMINO JOURNAL</span><h2>THƯ VIỆN KIẾN THỨC<br/>ĐANG ĐƯỢC HOÀN THIỆN.</h2><p>Nội dung sẽ tập trung vào những câu hỏi thật trước khi làm bếp.</p></div>}</section></main>}